const Customers = require('@/modules/catalogs/customers/models');
const User = require('@/modules/catalogs/users/models');

module.exports = async () => {
    // await Customers.sync({ force: true });
    // await User.sync({ force: true });
};